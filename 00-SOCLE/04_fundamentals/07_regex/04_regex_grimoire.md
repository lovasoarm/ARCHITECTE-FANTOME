---
stability: perissable_2027
last_reviewed: 2026-07
depends_on_vendor: false
acte: restituer
---

# Page verrouillée

> Rappel : ce grimoire simplifie via analogies. Lire d'abord [`05-MAITRISE/06_annexes/GRIMOIRE_CODE_HONNEUR.md`](../../../05-MAITRISE/06_annexes/18_GRIMOIRE_CODE_HONNEUR.md).

Temps de lecture ~9 min

> **Interdit de lire cette page avant d'avoir coché la checklist ci-dessous.**
> Un grimoire lu trop tôt donne l'illusion de savoir. C'est le pire piège pédagogique.

## Checklist prérequis

- [ ] J'ai fini **tous** les exercices du module courant.
- [ ] J'ai réussi le `00_prereq_check.md` du module suivant.
- [ ] J'ai écrit **au moins un** de mes propres exemples (pas copié).
- [ ] Je peux réexpliquer les 3 concepts phares du module **sans regarder**.

Si une seule case n'est pas cochée : ferme ce fichier. Reviens plus tard.

---

## GRIMOIRE DES REGEX

> Relevé le 2026-08-14, source : pages tarifaires publiques des fournisseurs citées dans le module, à revérifier avant 2027.

| Terme                           | Définition                                                                                                                                     | Code                                                                                                                | Analogies                                                                                                             | Limite |
| ------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ------- |
| **Regex**                       | Pattern de texte qui décrit une forme à reconnaître ou extraire dans une chaîne.                                                               | `/\d+/` matche un ou plusieurs chiffres                                                                             | un détecteur d'empreintes / un jutsu de reconnaissance qui cherche une signature précise                              | « un détecteur d'empreintes » n'a ni facture ni horloge ; sur Regex, le cas limite est rare en fréquence et dominant en coût, l'image raconte le cas courant. Vérifie le comportement sur vide, zéro et valeur nulle. |
| **Littéral regex**              | Définir une regex directement entre slashes. Analysée à la compilation, plus rapide.                                                           | `/pattern/flags`                                                                                                    | écrire un jutsu directement dans le parchemin / graver une règle dans la pierre                                       | « écrire un jutsu directement dans le parchemin » décrit un monde où chaque étape se voit ; sur Littéral regex, les bornes (premier, dernier, zéro, vide) concentrent la majorité des défauts. Injecte une entrée volontairement hostile. |
| **RegExp()**                    | Constructeur pour créer une regex depuis une chaîne dynamique. Utile quand le pattern n'est pas connu à l'avance.                              | `new RegExp(variable, "gi")`                                                                                        | forger une arme sur commande / adapter le jutsu selon la cible                                                        | « forger une arme sur commande » se corrige toute seule quand elle dérape ; sur RegExp(), une valeur `null`, vide ou nulle traverse le code sans lever d'erreur jusqu'au dernier étage. Documente ce que le code refuse, pas seulement ce qu'il accepte. |
| **Flag `i`**                    | Case-insensitive. La regex ignore la différence minuscule/majuscule.                                                                           | `/naruto/i` matche "Naruto", "NARUTO", "naruto"                                                                     | chercher sans se soucier des accents d'un nom / reconnaître un ninja en civil comme en uniforme                       | « chercher sans se soucier des accents d'un nom » tient tant que rien ne tombe en route ; sur Flag i, l'entrée hostile ne respecte aucune des hypothèses de format. Écris le test des bornes avant le test du cas nominal. |
| **Flag `g`**                    | Global. Trouve toutes les occurrences, pas seulement la première.                                                                              | `"aaa".match(/a/g)` → `["a","a","a"]`                                                                               | scanner tout le village, pas juste l'entrée / relire tout le rapport, pas juste le titre                              | « scanner tout le village, pas juste l'entrée » décrit un monde où chaque étape se voit ; sur Flag g, la conversion implicite change le résultat sans avertissement. Documente ce que le code refuse, pas seulement ce qu'il accepte. |
| **Flag `m`**                    | Multiline. `^` et `$` matchent chaque début/fin de ligne, pas juste la chaîne entière.                                                         | `/^\d/m` matche le premier chiffre de chaque ligne                                                                  | inspecter chaque étage d'un bâtiment / relire chaque fiche ninja séparément                                           | « inspecter chaque étage d'un bâtiment » se corrige toute seule quand elle dérape ; sur Flag m, un comportement non spécifié du moteur peut changer à la prochaine version. Injecte une entrée volontairement hostile. |
| **`\d`**                        | Classe de caractère : un chiffre entre 0 et 9. Équivalent à `[0-9]`.                                                                           | `/\d+/` matche "42"                                                                                                 | un chiffre dans le registre du village / un numéro de bib dans une course                                             | « un chiffre dans le registre du village » suppose un seul acteur à la fois ; sur \d, l'encodage des caractères casse au premier caractère hors ASCII. Écris le test des bornes avant le test du cas nominal. |
| **`\w`**                        | Classe de caractère : un caractère de mot : lettres, chiffres, underscore. Équivalent à `[a-zA-Z0-9_]`.                                        | `/\w+/` matche "naruto_123"                                                                                         | un caractère d'identifiant / une lettre dans un nom de code ninja                                                     | « un caractère d'identifiant » tient tant que rien ne tombe en route ; sur \w, l'encodage des caractères casse au premier caractère hors ASCII. Vérifie le comportement sur vide, zéro et valeur nulle. |
| **`\s`**                        | Classe de caractère : un espace blanc : espace, tab, saut de ligne.                                                                            | `/\s+/` matche des espaces multiples                                                                                | un silence dans un discours / une pause entre deux jutsu                                                              | « un silence dans un discours » décrit un monde où chaque étape se voit ; sur \s, l'encodage des caractères casse au premier caractère hors ASCII. Écris le test des bornes avant le test du cas nominal. |
| **`.` (point)**                 | N'importe quel caractère sauf le saut de ligne `\n` (sauf avec flag `s`).                                                                      | `/n.ruto/` matche "naruto" ou "n3ruto"                                                                              | un joker / "peu importe qui" dans une liste                                                                           | « un joker » tient tant que rien ne tombe en route ; sur . (point), le comportement dépend de l'ordre exact des opérations quand deux chemins se croisent. Injecte une entrée volontairement hostile. |
| **`^` et `$`**                  | Ancres : `^` = début de chaîne, `$` = fin de chaîne. Forcent le pattern à couvrir toute la chaîne avec `^...$`.                                | `/^\d+$/` valide que toute la chaîne est des chiffres                                                               | les gardes à l'entrée ET à la sortie du village / vérifier qu'un rapport commence ET finit correctement               | « les gardes à l'entrée ET à la sortie du village » décrit un monde où chaque étape se voit ; sur ^ et $, les bornes (premier, dernier, zéro, vide) concentrent la majorité des défauts. Documente ce que le code refuse, pas seulement ce qu'il accepte. |
| **`+`**                         | Quantificateur : 1 ou plusieurs fois. La chose doit apparaître au moins une fois.                                                              | `/\d+/` matche "1", "42", "9999"                                                                                    | au moins un ninja dans l'équipe / pas de mission si l'équipe est vide                                                 | « au moins un ninja dans l'équipe » suppose un seul acteur à la fois ; sur +, l'entrée hostile ne respecte aucune des hypothèses de format. Vérifie le comportement sur vide, zéro et valeur nulle. |
| **`*`**                         | Quantificateur : 0 ou plusieurs fois. La chose peut ne pas apparaître.                                                                         | `/\d*/` matche "", "0", "123"                                                                                       | une équipe qui peut être vide / un champ optionnel dans un formulaire                                                 | « une équipe qui peut être vide » a une frontière visible à l'oeil ; sur , un comportement non spécifié du moteur peut changer à la prochaine version. Documente ce que le code refuse, pas seulement ce qu'il accepte. |
| **`?`**                         | Quantificateur : 0 ou 1 fois. Rend une partie du pattern optionnelle.                                                                          | `/colou?r/` matche "color" et "colour"                                                                              | une armure optionnelle / un "s" de pluriel qui peut ou non être là                                                    | « une armure optionnelle » se corrige toute seule quand elle dérape ; sur ?, une valeur `null`, vide ou nulle traverse le code sans lever d'erreur jusqu'au dernier étage. Injecte une entrée volontairement hostile. |
| **`{n,m}`**                     | Quantificateur : entre n et m fois.                                                                                                            | `/\d{4}/` exactement 4 chiffres, `/\d{2,4}/` de 2 à 4                                                               | entre 2 et 4 ninjas dans une équipe / un code entre 4 et 6 caractères                                                 | « entre 2 et 4 ninjas dans une équipe » suppose que quelqu'un surveille ; sur {n,m}, le comportement dépend de l'ordre exact des opérations quand deux chemins se croisent. Vérifie le comportement sur vide, zéro et valeur nulle. |
| **Groupe `()`**                 | Regroupe une partie du pattern. Capture le contenu dans `match[1]`, `match[2]`, etc.                                                           | `"2024-04".match(/(\d{4})-(\d{2})/)` → `match[1]` = "2024"                                                          | un groupe de ninjas identifié / isoler la partie importante d'un rapport                                              | « un groupe de ninjas identifié » a une frontière visible à l'oeil ; sur Groupe (), la conversion implicite change le résultat sans avertissement. Écris le test des bornes avant le test du cas nominal. |
| **Groupe nommé `(?<nom>)`**     | Groupe capturant avec un nom. Accessible via `match.groups.nom`.                                                                               | `/(?<annee>\d{4})/` puis `match.groups.annee`                                                                       | donner un nom de code à chaque équipe / nommer chaque case d'un formulaire                                            | « donner un nom de code à chaque équipe » suppose un seul acteur à la fois ; sur Groupe nommé (?<nom>), le cas limite est rare en fréquence et dominant en coût, l'image raconte le cas courant. Injecte une entrée volontairement hostile. |
| **Groupe non-capturant `(?:)`** | Regroupe sans capturer dans les résultats. Pour les quantificateurs ou l'alternance sans stocker.                                              | `/(?:na)+/` matche "nanana" sans stocker                                                                            | coordonner un groupe sans le répertorier / regrouper pour l'ordre sans créer un dossier                               | « coordonner un groupe sans le répertorier » décrit un monde où chaque étape se voit ; sur Groupe non-capturant (?:), la conversion implicite change le résultat sans avertissement. Écris le test des bornes avant le test du cas nominal. |
| **Alternance `\|`**             | OU logique entre deux patterns.                                                                                                                | `/chat\|chien/` matche "chat" ou "chien" | choisir entre deux techniques / soit Naruto soit Sasuke sur la mission | « choisir entre deux techniques » n'a ni facture ni horloge ; sur Alternance |, le comportement dépend de l'ordre exact des opérations quand deux chemins se croisent. Vérifie le comportement sur vide, zéro et valeur nulle. |
| **Classe `[abc]`**              | Un seul caractère parmi ceux listés.                                                                                                           | `/[aeiou]/` matche une voyelle                                                                                      | une liste blanche de caractères autorisés / les ninjas de la liste de recrutement                                     | « une liste blanche de caractères autorisés » se corrige toute seule quand elle dérape ; sur Classe [abc], l'encodage des caractères casse au premier caractère hors ASCII. Documente ce que le code refuse, pas seulement ce qu'il accepte. |
| **Classe négative `[^abc]`**    | Un seul caractère absent de la liste.                                                                                                          | `/[^aeiou]/` matche une consonne                                                                                    | une liste noire / tout sauf les ninjas du village ennemi                                                              | « une liste noire » a une frontière visible à l'oeil ; sur Classe négative [^abc], l'encodage des caractères casse au premier caractère hors ASCII. Injecte une entrée volontairement hostile. |
| **`.test()`**                   | Méthode regex : retourne `true` si le pattern est trouvé, `false` sinon.                                                                       | `/\d+/.test("abc")` → `false`                                                                                       | le détecteur qui répond oui ou non / test de compétence : reçu ou recalé                                              | « le détecteur qui répond oui ou non » raconte le cas nominal ; sur .test(), un comportement non spécifié du moteur peut changer à la prochaine version. Écris le test des bornes avant le test du cas nominal. |
| **`.match()`**                  | Méthode string : retourne le(s) match(es) dans un tableau, ou `null` si rien trouvé. Toujours vérifier `null` avant d'utiliser.                | `"abc123".match(/\d+/)` → `["123"]`                                                                                 | récolter les preuves trouvées / retourner les éléments filtrés d'un rapport                                           | « récolter les preuves trouvées » suppose un seul acteur à la fois ; sur .match(), une valeur `null`, vide ou nulle traverse le code sans lever d'erreur jusqu'au dernier étage. Écris le test des bornes avant le test du cas nominal. |
| **`.matchAll()`**               | Méthode string : retourne un iterator de tous les matches avec leurs groupes. Nécessite le flag `g`.                                           | `[..."abc".matchAll(/./g)]`                                                                                         | scanner tout le texte et collecter chaque indice avec son contexte / inspecter chaque ligne d'un log avec ses détails | « scanner tout le texte et collecter chaque indice avec son contexte » tient tant que rien ne tombe en route ; sur .matchAll(), les bornes (premier, dernier, zéro, vide) concentrent la majorité des défauts. Documente ce que le code refuse, pas seulement ce qu'il accepte. |
| **`.replace()`**                | Méthode string : remplace le(s) match(es). Accepte une chaîne ou une fonction comme remplacement.                                              | `"ab".replace(/a/, "x")` → `"xb"`                                                                                   | effacer et réécrire une partie du parchemin / corriger une erreur dans un rapport                                     | « effacer et réécrire une partie du parchemin » n'a ni facture ni horloge ; sur .replace(), le cas limite est rare en fréquence et dominant en coût, l'image raconte le cas courant. Documente ce que le code refuse, pas seulement ce qu'il accepte. |
| **`.split()`**                  | Méthode string : découpe la chaîne sur le pattern.                                                                                             | `"a,b,,c".split(/,+/)` → `["a","b","c"]`                                                                            | couper le parchemin aux points de rupture / séparer une liste par ses délimiteurs                                     | « couper le parchemin aux points de rupture » suppose un seul acteur à la fois ; sur .split(), l'entrée hostile ne respecte aucune des hypothèses de format. Vérifie le comportement sur vide, zéro et valeur nulle. |
| **Lookahead `(?=...)`**         | Assertion positive : matche si suivi du pattern, sans consommer les caractères.                                                                | `/\d+(?=€)/` matche "42" dans "42€" sans inclure le "€"                                                             | vérifier ce qui suit une signature sans la lire / confirmer la présence d'un garde avant d'entrer                     | « vérifier ce qui suit une signature sans la lire » décrit un monde où chaque étape se voit ; sur Lookahead (?=...), l'encodage des caractères casse au premier caractère hors ASCII. Vérifie le comportement sur vide, zéro et valeur nulle. |
| **Lookbehind `(?<=...)`**       | Assertion positive : matche si précédé du pattern, sans consommer les caractères.                                                              | `/(?<=prix:\s)\d+/` matche le nombre après "prix: "                                                                 | vérifier l'origine avant de lire le message / n'écouter que si c'est un Hokage qui parle                              | « vérifier l'origine avant de lire le message » n'a ni facture ni horloge ; sur Lookbehind (?<=...), le comportement dépend de l'ordre exact des opérations quand deux chemins se croisent. Injecte une entrée volontairement hostile. |
| **ReDoS**                       | Backtracking catastrophique : certains patterns + certaines chaînes provoquent une explosion du temps d'exécution. Risque de sécurité en prod. | `/^(a+)+$/` sur "aaaab" peut bloquer le thread                                                                      | un piège à ninja qui se referme lentement / une boucle infinie qui mange le CPU                                       | « un piège à ninja qui se referme lentement » s'arrête à la première surprise ; sur ReDoS, les bornes (premier, dernier, zéro, vide) concentrent la majorité des défauts. Écris le test des bornes avant le test du cas nominal. |
| **`lastIndex`**                 | Propriété d'une regex avec flag `g` : indique où reprendre le prochain `.exec()`. Peut causer des bugs si la même regex est réutilisée.        | `regex.lastIndex = 0` pour reset manuellement                                                                       | le signet d'un inspecteur qui reprend où il s'est arrêté / la mémoire de position d'un jutsu de recherche             | « le signet d'un inspecteur qui reprend où il s'est arrêté » s'arrête à la première surprise ; sur lastIndex, un comportement non spécifié du moteur peut changer à la prochaine version. Documente ce que le code refuse, pas seulement ce qu'il accepte. |

---

## OÙ L'ANALOGIE CASSE

Rappel Partie B.2 : toute analogie de ce grimoire simplifie un mécanisme.
Quand tu dois **décider** (fix, refactor, ADR), retourne au mécanisme réel,
pas à l'image. L'analogie sert à comprendre vite ; elle ment toujours un peu.

---

## OÙ LES ANALOGIES CASSENT (règle B.2)

Les analogies de ce grimoire simplifient : elles ne définissent pas. Une
closure **nest pas** un tiroir ; un event loop **nest pas** un carrousel ;
une pile **nest pas** une pile de crêpes. Chaque analogie sert à visualiser
un mécanisme ; elle cesse dès que tu veux raisonner sur la complexité, la
mémoire, la concurrence ou les cas limites. Reviens toujours à la définition
technique avant de coder, débugger ou expliquer à un pair. Une analogie
prise pour la réalité devient un obstacle épistémologique.
