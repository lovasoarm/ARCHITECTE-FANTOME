---
stability: intemporel
last_reviewed: 2026-07
depends_on_vendor: false
acte: restituer
---

# Page verrouillée

> Rappel : ce grimoire simplifie via analogies. Lire d'abord [`05-MAITRISE/06_annexes/GRIMOIRE_CODE_HONNEUR.md`](../../05-MAITRISE/06_annexes/18_GRIMOIRE_CODE_HONNEUR.md).

Temps de lecture ~11 min

> **Interdit de lire cette page avant d'avoir coché la checklist ci-dessous.**
> Un grimoire lu trop tôt donne l'illusion de savoir. C'est le pire piège pédagogique.

## Checklist prérequis

- [ ] J'ai fini **tous** les exercices du module courant.
- [ ] J'ai réussi le `00_prereq_check.md` du module suivant.
- [ ] J'ai écrit **au moins un** de mes propres exemples (pas copié).
- [ ] Je peux réexpliquer les 3 concepts phares du module **sans regarder**.

Si une seule case n'est pas cochée : ferme ce fichier. Reviens plus tard.

---

## MATH GRIMOIRE : 05_MATH_BASICS

Les maths qu'un dev croise vraiment. Pas celles d'un manuel de terminale.
Chaque terme ici apparaît dans du vrai code, dans de vraies prods, dans de vrais bugs.

---

| Terme                                      | Définition                                                                                                                                                                                                                                                                                                      | Code                                                                                                                                                             | Analogies                                                                                                                           | Limite |
| ------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- | ------- |
| **Opérateur logique AND**                  | Retourne `true` si les deux opérandes sont vrais. En JS, retourne la première valeur falsy ou la dernière valeur.                                                                                                                                                                                               | `true && "ok"` → `"ok"` / `null && fn()` → `null`                                                                                                                | Verrou à deux clés : les deux doivent tourner / Deux gardiens : les deux doivent valider                                            | « Verrou à deux clés : les deux doivent tourner » se rejoue à l'identique, le code non ; sur Opérateur logique AND, un dépassement de précision au-delà de 2^53 ne lève aucune erreur, il renvoie une valeur fausse. Vérifie sur trois valeurs limites avant d'industrialiser la formule. |
| **Opérateur logique OR**                   | Retourne `true` si au moins un opérande est vrai. En JS, retourne la première valeur truthy ou la dernière.                                                                                                                                                                                                     | `null \|\| "défaut"` → `"défaut"` / `"val" \|\| "défaut"` → `"val"` | Porte avec deux entrées : une seule suffit / Sélection du premier candidat disponible | « Porte avec deux entrées : une seule suffit » suppose un seul acteur à la fois ; sur Opérateur logique OR, un dépassement de précision au-delà de 2^53 ne lève aucune erreur, il renvoie une valeur fausse. Vérifie sur trois valeurs limites avant d'industrialiser la formule. |
| **Court-circuit (short-circuit)**          | `&&` arrête dès qu'il trouve un falsy. `\|\| ` arrête dès qu'il trouve un truthy. La suite n'est pas évaluée.                                                                                                                                                                                             | `user && user.nom` : si `user` est null, `user.nom` n'est jamais lu                                                                                             | Sécurité de Kakashi qui n'ouvre pas le Sharingan si ennemi déjà KO / Fuse électrique qui coupe avant que ça brûle                  | « Sécurité de Kakashi qui n'ouvre pas le Sharingan si ennemi déjà KO » s'arrête à la première surprise ; sur Court-circuit (short-circuit), les nombres flottants IEEE 754 accumulent une erreur qui ne se voit qu'à la troisième opération. Affiche le calcul intermédiaire avant de faire confiance au total. |
| **Nullish coalescing `??`**                | Retourne le côté droit uniquement si le gauche est `null` ou `undefined`. Contrairement à `\|\| `, laisse passer `0`, `""`, `false`.                                                                                                                                                                          | `score ?? 0` : si score = 0, retourne 0 (pas le fallback)                                                                                                        | Différence entre "absent" et "vide" / Remplaçant qui entre uniquement si le titulaire est blessé, pas juste fatigué                 | « Différence entre "absent" et "vide" » n'a ni facture ni horloge ; sur Nullish coalescing ??, l'arrondi appliqué à l'affichage n'est pas l'arrondi appliqué au calcul stocké. Travaille en entiers (centimes, millisecondes) quand l'argent ou le temps sont en jeu. |
| **Valeurs falsy**                          | Les 8 valeurs que JS considère comme `false` dans un contexte booléen : `false`, `0`, `-0`, `0n`, `""`, `null`, `undefined`, `NaN`.                                                                                                                                                                             | `if (0)` ne s'exécute pas / `Boolean("")` → `false`                                                                                                              | Les 8 disqualifiés du match / Les zéros qui ne comptent pas dans le classement                                                      | « Les 8 disqualifiés du match » raconte le cas nominal ; sur Valeurs falsy, un dépassement de précision au-delà de 2^53 ne lève aucune erreur, il renvoie une valeur fausse. Vérifie sur trois valeurs limites avant d'industrialiser la formule. |
| **Lois de De Morgan**                      | `!(A && B)` est équivalent à `(!A \|\| !B)`. `!(A \|\| B)` est équivalent à `(!A && !B)`. Permet de simplifier les conditions complexes.                                                                                                                                                                    | `!(estAdmin && estActif)` ↔ `!estAdmin \|\| !estActif`                                                                                                          | Inverser "les deux gagnent" = "au moins un perd" / Négation du contrat collectif                                                    | « Inverser "les deux gagnent" = "au moins un perd" » suppose que quelqu'un surveille ; sur Lois de De Morgan, une distribution est décrite par sa médiane et ses percentiles, pas par sa moyenne. Compare ton résultat à un calcul fait à la main sur un petit échantillon. |
| **Modulo `%`**                             | Retourne le reste de la division entière. Si `a % n`, le résultat est dans `[0, n-1]` pour des positifs.                                                                                                                                                                                                        | `7 % 3` → `1` / `index % tableau.length` → index cyclique                                                                                                        | Aiguille d'une horloge : toujours entre 0 et 11 / Rotation de garde : chaque soldat reprend son tour                                | « Aiguille d'une horloge : toujours entre 0 et 11 » suppose un seul acteur à la fois ; sur Modulo %, l'ordre des opérations change le résultat dès qu'on mélange grands et petits nombres. Compare ton résultat à un calcul fait à la main sur un petit échantillon. |
| **Arithmétique modulaire**                 | Système de calcul où les nombres "bouclent" après avoir atteint un modulus. Utilisé pour les cycles, la pagination, les ring buffers.                                                                                                                                                                           | `(currentPage + 1) % totalPages` → page suivante cyclique                                                                                                        | Les heures sur un cadran : après 23h vient 0h / Saisons : après l'automne revient le printemps                                      | « Les heures sur un cadran : après 23h vient 0h » tient tant que rien ne tombe en route ; sur Arithmétique modulaire, l'ordre des opérations change le résultat dès qu'on mélange grands et petits nombres. Affiche le calcul intermédiaire avant de faire confiance au total. |
| **Opérateurs bitwise**                     | Opèrent bit par bit sur la représentation binaire d'un entier 32 bits. `&`, `\|`, `^`, `~`, `<<`, `>>`, `>>>`.                                                                                                                                                                                                  | `0b1010 & 0b1100` → `0b1000` (8) | Interrupteurs électriques en parallèle ou en série / Masques de découpe sur une image                                         | « Interrupteurs électriques en parallèle ou en série » se corrige toute seule quand elle dérape ; sur Opérateurs bitwise, le modulo sur des négatifs ne suit pas l'intuition mathématique scolaire. Travaille en entiers (centimes, millisecondes) quand l'argent ou le temps sont en jeu. |
| **Masque de bits (bitmask)**               | Un entier utilisé pour tester, activer ou désactiver des bits spécifiques via `&`, `\|`, `^`. Système de flags compacts.                                                                                                                                                                                        | `const ADMIN = 1 << 2` / `perms & ADMIN` → 0 ou 4 | Badge d'accès à étages multiples / Carte perforée : chaque trou = une permission                             | « Badge d'accès à étages multiples » suppose que quelqu'un surveille ; sur Masque de bits (bitmask), une division par zéro renvoie `Infinity` au lieu d'échouer. Travaille en entiers (centimes, millisecondes) quand l'argent ou le temps sont en jeu. |
| **XOR `^`**                                | Retourne 1 uniquement si les bits sont différents. Utile pour toggle, swap sans variable temporaire, comparaison.                                                                                                                                                                                               | `a ^= 1` toggle le bit 0 / `a ^ b ^ a` → `b`                                                                                                                     | Interrupteur va-et-vient : deux switches, comportement inverse / Détective : vrai si exactement l'un des deux ment                  | « Interrupteur va-et-vient : deux switches, comportement inverse » a une frontière visible à l'oeil ; sur XOR ^, un pourcentage moyen de moyennes n'est pas la moyenne réelle. Travaille en entiers (centimes, millisecondes) quand l'argent ou le temps sont en jeu. |
| **Left shift `<<`**                        | Décale les bits vers la gauche de N positions. Équivaut à multiplier par `2^N`. Plus rapide que `Math.pow`.                                                                                                                                                                                                     | `1 << 3` → `8` / `1 << 10` → `1024`                                                                                                                              | Doubler la mise N fois / Ajouter N zéros à la fin d'un nombre binaire                                                               | « Doubler la mise N fois » suppose que quelqu'un surveille ; sur Left shift <<, le modulo sur des négatifs ne suit pas l'intuition mathématique scolaire. Affiche le calcul intermédiaire avant de faire confiance au total. |
| **Fonction de hachage**                    | Transforme une donnée de taille arbitraire en une valeur de taille fixe. Déterministe, rapide, non réversible.                                                                                                                                                                                                  | `hash("Naruto")` → `2847361` (toujours le même)                                                                                                                  | Empreinte digitale d'un fichier / Code barre : résume l'objet en une suite de chiffres                                              | « Empreinte digitale d'un fichier » décrit un monde où chaque étape se voit ; sur Fonction de hachage, un dépassement de précision au-delà de 2^53 ne lève aucune erreur, il renvoie une valeur fausse. Vérifie sur trois valeurs limites avant d'industrialiser la formule. |
| **Collision de hash**                      | Deux entrées différentes qui produisent le même hash. Inévitable (pigeonhole principle). Les bonnes fonctions les rendent improbables.                                                                                                                                                                          | `hash("abc") === hash("bca")` (mauvaise fonction)                                                                                                                | Deux joueurs avec le même numéro de maillot / Deux suspects avec le même ADN : ça n'arrive pas avec une bonne analyse               | « Deux joueurs avec le même numéro de maillot » décrit un monde où chaque étape se voit ; sur Collision de hash, les nombres flottants IEEE 754 accumulent une erreur qui ne se voit qu'à la troisième opération. Compare ton résultat à un calcul fait à la main sur un petit échantillon. |
| **Distribution de hash**                   | Qualité d'une fonction de hash : elle doit répartir uniformément les entrées sur toutes les sorties possibles.                                                                                                                                                                                                  | Vérifier avec un histogramme de fréquences                                                                                                                       | Tirage au sort équitable : chaque numéro a la même chance / Répartition de charge entre serveurs                                    | « Tirage au sort équitable : chaque numéro a la même chance » se corrige toute seule quand elle dérape ; sur Distribution de hash, une distribution est décrite par sa médiane et ses percentiles, pas par sa moyenne. Compare ton résultat à un calcul fait à la main sur un petit échantillon. |
| **`Math.random()`**                        | Retourne un nombre flottant dans `[0, 1)`. Distribution uniforme sur cet intervalle. Pas cryptographiquement sûr.                                                                                                                                                                                               | `Math.floor(Math.random() * 6) + 1` → dé à 6 faces                                                                                                               | Roulette avec 100 cases égales / RNG basique d'un jeu vidéo                                                                         | « Roulette avec 100 cases égales » tient tant que rien ne tombe en route ; sur Math.random(), une distribution est décrite par sa médiane et ses percentiles, pas par sa moyenne. Affiche le calcul intermédiaire avant de faire confiance au total. |
| **Distribution uniforme**                  | Chaque valeur possible a la même probabilité d'apparaître. `Math.random()` en est une approximation pour `[0,1)`.                                                                                                                                                                                               | `Math.floor(Math.random() * n)` → uniforme sur `[0, n-1]`                                                                                                        | Chaque joueur a autant de chances d'être sélectionné / Tirage au sort sans favoritisme                                              | « Chaque joueur a autant de chances d'être sélectionné » tient tant que rien ne tombe en route ; sur Distribution uniforme, une distribution est décrite par sa médiane et ses percentiles, pas par sa moyenne. Vérifie sur trois valeurs limites avant d'industrialiser la formule. |
| **Probabilité pondérée (weighted random)** | Chaque option a une probabilité différente. S'implémente avec des seuils cumulés ou des tableaux pondérés.                                                                                                                                                                                                      | `[{item:"épée",w:70},{item:"légendaire",w:5}]` + seuil                                                                                                           | Gacha avec des taux différents par rareté / Drop rate en RPG : le boss rare lâche rarement son item rare                            | « Gacha avec des taux différents par rareté » tient tant que rien ne tombe en route ; sur Probabilité pondérée (weighted random), l'arrondi appliqué à l'affichage n'est pas l'arrondi appliqué au calcul stocké. Compare ton résultat à un calcul fait à la main sur un petit échantillon. |
| **Seed (graine RNG)**                      | Valeur initiale d'un générateur pseudo-aléatoire. Même seed → même séquence. Utile pour les tests reproductibles.                                                                                                                                                                                               | `mulberry32(42)` → toujours la même séquence                                                                                                                     | Recette secrète : même ingrédients → même plat / Niveau généré procéduralement : même seed = même map                               | « Recette secrète : même ingrédients → même plat » tient tant que rien ne tombe en route ; sur Seed (graine RNG), une division par zéro renvoie `Infinity` au lieu d'échouer. Vérifie sur trois valeurs limites avant d'industrialiser la formule. |
| **Vecteur**                                | Une quantité avec une direction et une magnitude. Représenté par `{x, y}` en 2D. Différent d'un point : c'est un déplacement.                                                                                                                                                                                   | `{x: 3, y: 4}` magnitude = 5 / représente "bouge de 3 droite, 4 bas"                                                                                             | Flèche sur une carte : longueur = distance, pointe = direction / Shoot de Lewa : force + trajectoire                                | « Flèche sur une carte : longueur = distance, pointe = direction » suppose un seul acteur à la fois ; sur Vecteur, un pourcentage moyen de moyennes n'est pas la moyenne réelle. Affiche le calcul intermédiaire avant de faire confiance au total. |
| **Normalisation d'un vecteur**             | Diviser un vecteur par sa magnitude pour obtenir un vecteur de longueur 1. Donne une direction pure sans la vitesse.                                                                                                                                                                                            | `{x: v.x / mag, y: v.y / mag}` avec `mag = sqrt(x²+y²)`                                                                                                          | GPS qui donne la direction sans la distance / Boussole : seulement le cap, pas le kilométrage                                       | « GPS qui donne la direction sans la distance » décrit un monde où chaque étape se voit ; sur Normalisation d'un vecteur, l'arrondi appliqué à l'affichage n'est pas l'arrondi appliqué au calcul stocké. Travaille en entiers (centimes, millisecondes) quand l'argent ou le temps sont en jeu. |
| **Jutsu scalaire (dot product)**           | `v1.x * v2.x + v1.y * v2.y`. Retourne un scalaire. Positif = même sens, 0 = perpendiculaire, négatif = sens opposé.                                                                                                                                                                                             | `dotProduct(regard, dirEnnemie)` → l'ennemi est-il dans le FOV ?                                                                                                 | Accord entre deux votes : +1 = d'accord, 0 = neutre, -1 = opposés / Phares qui s'alignent ou se croisent                            | « Accord entre deux votes : +1 = d'accord, 0 = neutre, -1 = opposés » se corrige toute seule quand elle dérape ; sur Jutsu scalaire (dot product), le modulo sur des négatifs ne suit pas l'intuition mathématique scolaire. Travaille en entiers (centimes, millisecondes) quand l'argent ou le temps sont en jeu. |
| **Distance euclidienne**                   | Distance "à vol d'oiseau" entre deux points. `sqrt((x2-x1)² + (y2-y1)²)`. Pythagore appliqué.                                                                                                                                                                                                                   | `Math.sqrt(dx*dx + dy*dy)`                                                                                                                                       | Distance GPS entre deux villes / Passe longue en diagonale sur le terrain                                                           | « Distance GPS entre deux villes » décrit un monde où chaque étape se voit ; sur Distance euclidienne, l'arrondi appliqué à l'affichage n'est pas l'arrondi appliqué au calcul stocké. Affiche le calcul intermédiaire avant de faire confiance au total. |
| **Distance Manhattan**                     | Somme des distances absolues sur chaque axe. `\|x2-x1\| + \|y2-y1\|`. Distance réelle si on ne peut se déplacer qu'en horizontal/vertical.                                                                                                                                                                  | `Math.abs(dx) + Math.abs(dy)`                                                                                                                                    | Nombre de rues à traverser dans une ville en grille / Déplacements sur un échiquier sans diagonale                                  | « Nombre de rues à traverser dans une ville en grille » suppose que quelqu'un surveille ; sur Distance Manhattan, un pourcentage moyen de moyennes n'est pas la moyenne réelle. Compare ton résultat à un calcul fait à la main sur un petit échantillon. |
| **Distance au carré**                      | `dx² + dy²` sans `sqrt`. Utilisée pour comparer des distances sans besoin de la valeur exacte. 2x plus rapide.                                                                                                                                                                                                  | `if (dist2(A,C) < dist2(B,C))` → A est plus proche                                                                                                               | Comparer des poids sans balance précise : le plus lourd reste le plus lourd / Classement sans note exacte                           | « Comparer des poids sans balance précise : le plus lourd reste le... » suppose que quelqu'un surveille ; sur Distance au carré, un dépassement de précision au-delà de 2^53 ne lève aucune erreur, il renvoie une valeur fausse. Vérifie sur trois valeurs limites avant d'industrialiser la formule. |
| **Interpolation linéaire (lerp)**          | Calcule une valeur entre `a` et `b` à hauteur de `t` (entre 0 et 1). `a + (b - a) * t`.                                                                                                                                                                                                                         | `lerp(0, 100, 0.3)` → 30 / `lerp(rouge, bleu, 0.5)` → violet                                                                                                     | Fondu entre deux scènes / Caméra qui glisse progressivement vers le joueur                                                          | « Fondu entre deux scènes » décrit un monde où chaque étape se voit ; sur Interpolation linéaire (lerp), les nombres flottants IEEE 754 accumulent une erreur qui ne se voit qu'à la troisième opération. Vérifie sur trois valeurs limites avant d'industrialiser la formule. |
| **Bounding box**                           | Rectangle minimal qui englobe un objet. Utilisé comme première approximation pour les tests de collision. Rapide mais imprécis.                                                                                                                                                                                 | `a.x < b.x + b.w && a.x + a.w > b.x && ...`                                                                                                                      | Emballage carton d'un objet bizarre : si deux cartons ne se touchent pas, les objets non plus / Zone de détection rapide d'un radar | « Emballage carton d'un objet bizarre : si deux cartons ne se... » tient tant que rien ne tombe en route ; sur Bounding box, l'ordre des opérations change le résultat dès qu'on mélange grands et petits nombres. Travaille en entiers (centimes, millisecondes) quand l'argent ou le temps sont en jeu. |
| **`Math.atan2(y, x)`**                     | Retourne l'angle (en radians) entre l'axe X positif et le point `(x, y)`. Gère les quatre quadrants. Toujours préférer à `Math.atan`.                                                                                                                                                                           | `Math.atan2(dy, dx)` → angle vers une cible                                                                                                                      | Boussole qui sait où est le nord peu importe ta direction / GPS qui calcule le cap sans se perdre en quadrant                       | « Boussole qui sait où est le nord peu importe ta direction » suppose un seul acteur à la fois ; sur Math.atan2(y, x), une division par zéro renvoie `Infinity` au lieu d'échouer. Compare ton résultat à un calcul fait à la main sur un petit échantillon. |
| **Heatmap**                                | Grille qui agrège des points dans des cellules. Chaque cellule comptabilise combien de points elle contient.                                                                                                                                                                                                    | `grille[Math.floor(y/res)][Math.floor(x/res)]++`                                                                                                                 | Carte de chaleur d'un match : zones de pressing / Analytics UX : quelles zones d'une page cliquent vraiment les shinobis            | « Carte de chaleur d'un match : zones de pressing » suppose que quelqu'un surveille ; sur Heatmap, le modulo sur des négatifs ne suit pas l'intuition mathématique scolaire. Affiche le calcul intermédiaire avant de faire confiance au total. |

---

→ Leçon complète sur les probabilités et le RNG : `05_probability_random.md`
→ Leçon complète sur les opérateurs bitwise et les flags : `03_bit_manipulation.md`
→ Leçon complète sur l'arithmétique modulaire : `02_modular_arithmetic.md`
→ Leçon complète sur les fonctions de hachage : `04_hashing_basics.md`

---

## OÙ L'ANALOGIE CASSE

Rappel Partie B.2 : toute analogie de ce grimoire simplifie un mécanisme.
Quand tu dois **décider** (fix, refactor, ADR), retourne au mécanisme réel,
pas à l'image. L'analogie sert à comprendre vite ; elle ment toujours un peu.

---

## OÙ LES ANALOGIES CASSENT (règle B.2)

Les analogies de ce grimoire simplifient : elles ne définissent pas. Une
closure **nest pas** un tiroir ; un event loop **nest pas** un carrousel ;
une pile **nest pas** une pile de crêpes. Chaque analogie sert à visualiser
un mécanisme ; elle cesse dès que tu veux raisonner sur la complexité, la
mémoire, la concurrence ou les cas limites. Reviens toujours à la définition
technique avant de coder, débugger ou expliquer à un pair. Une analogie
prise pour la réalité devient un obstacle épistémologique.
